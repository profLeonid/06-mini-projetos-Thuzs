'use strict'

function calcularDesconto ( ){
    const preco_original = document.getElementById('preco_original')
    const desconto = document.getElementById('desconto')

    desconto / 100 * preco_original
}